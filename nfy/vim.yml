vim:
        check: "vim -h"
        install_apt:
                script: "apt-get install -y vim"
        deps:
                - apt
        install_brew:
                script: "apt-get install -y vim"
        deps:
                - apt
